name: "ci-ext-eac (stage 1-7)"

on:
  workflow_call:
    inputs:
      ref:
        required: false
        type: string
        default: ''
      sha:
        required: false
        type: string
        description: 'Commit SHA to test'
        default: ''
      trigger_run_id:
        required: false
        type: string
        description: 'Run ID of trigger workflow (for artifact download)'
        default: ''
  workflow_dispatch:
    inputs:
      ref:
        required: false
        type: string
        description: 'Git ref to checkout'
        default: ''
      sha:
        required: false
        type: string
        description: 'Commit SHA to test'
        default: ''
      trigger_run_id:
        required: false
        type: string
        description: 'Run ID of trigger workflow (for artifact download)'
        default: ''

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write
  actions: read

jobs:
  # =============================================================================
  # Build ext-eac container
  # =============================================================================
  build:
    name: Build (ext-eac)
    runs-on: ubuntu-latest
    outputs:
      image-name: ${{ steps.container.outputs.image-name }}
      image-tag: ${{ steps.container.outputs.image-tag }}
      short-sha: ${{ steps.container.outputs.short-sha }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref || github.ref }}

      - name: Build Container
        id: container
        uses: ./.github/actions/build-container
        with:
          module: ext-eac
          trigger-run-id: ${{ inputs.trigger_run_id }}
          commit-sha: ${{ inputs.sha || github.sha }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # =============================================================================
  # Run module tests (specs/unit tests if defined)
  # =============================================================================
  test:
    name: Test (ext-eac)
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref || github.ref }}

      - name: Test Module
        uses: ./.github/actions/test-module
        with:
          module: ext-eac
          trigger-run-id: ${{ inputs.trigger_run_id }}
          suites: unit,integration
          results-artifact-name: test-results-ext-eac

  # =============================================================================
  # Container tests with r2r CLI and direct Docker
  # =============================================================================
  integration-test:
    name: Container Test (ext-eac)
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref || github.ref }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull CI image from GHCR
        env:
          IMAGE_NAME: ${{ needs.build.outputs.image-name }}
          IMAGE_TAG: ${{ needs.build.outputs.image-tag }}
        run: |
          echo "Pulling CI image..."
          docker pull "$IMAGE_NAME:$IMAGE_TAG"
          docker tag "$IMAGE_NAME:$IMAGE_TAG" ext-eac:ci-test
          docker images ext-eac:ci-test

      - name: Download Build Artifacts
        uses: actions/download-artifact@v7
        with:
          name: build-artifacts-ext-eac
          path: out/build/

      - name: Download r2r-cli for testing
        uses: ./.github/actions/download-module-deps
        with:
          module: r2r-cli
          commit-sha: ${{ inputs.sha || github.sha }}

      - name: Setup r2r CLI
        run: |
          echo "Setting up r2r CLI from build artifacts..."
          cp out/build/r2r-cli/r2r-linux-amd64 ./r2r
          chmod +x ./r2r
          echo "✓ r2r-cli ready"

      - name: Test Extension via r2r CLI
        run: |
          echo "Testing eac extension through r2r CLI..."

          # Create isolated test environment
          mkdir -p /tmp/ext-eac-test
          cd /tmp/ext-eac-test
          mkdir -p .git
          cp -r $GITHUB_WORKSPACE/contracts ./contracts

          # Configure to use CI test image
          mkdir -p .r2r
          cat > .r2r/r2r-cli.yml << EOF
          extensions:
            - name: 'eac'
              image: 'ext-eac:ci-test'
              image_pull_policy: 'Never'
          EOF

          cp $GITHUB_WORKSPACE/r2r ./r2r
          chmod +x ./r2r

          echo ""
          echo "Testing 'show modules' command..."
          timeout 120 ./r2r run eac show modules

          echo ""
          echo "Testing 'extension-meta' command..."
          timeout 30 ./r2r run eac extension-meta

          echo ""
          echo "Extension tests passed"

      - name: Test Direct Docker Execution
        run: |
          echo "Testing extension directly with Docker..."

          echo ""
          echo "Test 1: Help command"
          docker run --rm -v $(pwd):/var/task ext-eac:ci-test help 2>&1 | head -20 || true

          echo ""
          echo "Test 2: Extension metadata"
          docker run --rm ext-eac:ci-test extension-meta

          echo ""
          echo "Direct Docker tests passed"

      - name: Test Summary
        if: success()
        env:
          IMAGE_NAME: ${{ needs.build.outputs.image-name }}
          IMAGE_TAG: ${{ needs.build.outputs.image-tag }}
        run: |
          echo "## Container Test: ext-eac" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All extension tests passed." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### Tests Executed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Test | Method | Description |" >> $GITHUB_STEP_SUMMARY
          echo "|------|--------|-------------|" >> $GITHUB_STEP_SUMMARY
          echo "| show modules | r2r CLI | List repository modules |" >> $GITHUB_STEP_SUMMARY
          echo "| extension-meta | r2r CLI | Extension metadata |" >> $GITHUB_STEP_SUMMARY
          echo "| help | Docker | Direct container execution |" >> $GITHUB_STEP_SUMMARY
          echo "| extension-meta | Docker | Direct container execution |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          echo "### Tested Image" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`$IMAGE_NAME:$IMAGE_TAG\`" >> $GITHUB_STEP_SUMMARY

  # =============================================================================
  # Build Evidence Books
  # =============================================================================
  evidence:
    name: Evidence (ext-eac)
    needs: [build, test, integration-test]
    if: success()
    runs-on: ubuntu-latest
    outputs:
      artifact-name: ${{ steps.evidence.outputs.artifact-name }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref || github.ref }}
          fetch-depth: 0
          fetch-tags: true

      - name: Build Evidence Books
        id: evidence
        uses: ./.github/actions/build-evidence-books
        with:
          module: ext-eac
          trigger-run-id: ${{ inputs.trigger_run_id }}

  # =============================================================================
  # Summary - Centralized status and failure handling
  # =============================================================================
  summary:
    name: CI Summary (ext-eac)
    needs: [build, test, integration-test, evidence]
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Setup Commands Binary
        id: commands
        uses: ./.github/actions/setup-commands

      - name: Generate Summary
        env:
          COMMANDS: ${{ steps.commands.outputs.commands-path }}
          BUILD_RESULT: ${{ needs.build.result }}
          TEST_RESULT: ${{ needs.test.result }}
          INTEGRATION_RESULT: ${{ needs.integration-test.result }}
          EVIDENCE_RESULT: ${{ needs.evidence.result }}
        run: |
          echo "## CI Summary: ext-eac" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          ${COMMANDS:?COMMANDS not set} show ci-summary --container --build=$BUILD_RESULT --container-test-enabled --container-test=$INTEGRATION_RESULT --evidence-enabled --evidence=$EVIDENCE_RESULT >> $GITHUB_STEP_SUMMARY

      - name: Diagnostic Link on Failure
        if: contains(needs.*.result, 'failure')
        env:
          COMMANDS: ${{ steps.commands.outputs.commands-path }}
        run: |
          ${COMMANDS:?COMMANDS not set} pipeline ci summary-link ${{ github.run_id }} --type container >> $GITHUB_STEP_SUMMARY
