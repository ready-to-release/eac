# yaml-language-server: $schema=../../contracts/eac-core/0.1.0/repository.schema.json
#
# Project-specific repository configuration overrides
# Defaults loaded from contracts/eac-core/0.1.0/defaults/repository.yml
#

repository:
  # This is a monorepo (default is poly)
  type: mono

# Override: Go-specific test implementation location
# (moved from modules.yml defaults since repository.yml is the correct location)
paths:
  test_impl_root: "go/eac/specs/impl"
