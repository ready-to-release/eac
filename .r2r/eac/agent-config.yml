# Team AI Configuration (Production/Shared)
# Generated by: init command
# SAFE TO COMMIT: Contains only environment variable references, not actual secrets
#
# This file is committed and shared across the team.
# Used by r2r CLI, CI/CD, and production workflows.
#
# For local development with Claude CLI (no API costs):
# - Run: .\importer.ps1
# - This creates: .r2r/eac/agent-config.personal.yml (gitignored)
# - Personal config takes precedence over this team config

provider:
  name: claude-api
  model: claude-3-haiku-20240307
  endpoint: https://api.anthropic.com/v1
  api_key: ${ANTHROPIC_API_KEY}
