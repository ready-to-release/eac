[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "data-store"
version = "1.0.0"
description = "Test key-value data store"
requires-python = ">=3.9"

[project.optional-dependencies]
dev = ["behave>=1.2", "pytest>=7.0"]
