modules:
  - moniker: api-gateway
    components:
      - type: go
        name: go
        root: go/api-gateway

  - moniker: web-frontend
    components:
      - type: typescript
        name: typescript
        root: typescript/web-frontend

  - moniker: ml-engine
    components:
      - type: python
        name: python
        root: python/ml-engine

  - moniker: core-services
    components:
      - type: dotnet
        name: dotnet
        root: dotnet/core-services

  - moniker: cli-tools
    components:
      - type: rust
        name: rust
        root: rust/cli-tools

  - moniker: containers
    components:
      - type: container
        name: container
        root: containers/images
