@deps:go @ov
Feature: src-commands_mock-feature

  As a developer
  I want to generate specifications
  So that I can document my features

  Background:
    Given the system is initialized

  Rule: Specifications must follow proper structure

    @L2 @ov
    Scenario: Basic specification generation works
      When I run the create spec command
      Then the specification is generated successfully
      And the file contains valid Gherkin syntax

    @L2 @ov
    Scenario: Multiple scenarios can be generated
      When I request multiple scenarios
      Then each scenario has proper tags
      And each scenario has Given/When/Then steps

  Rule: Generated specifications must be valid

    @L2 @ov
    Scenario: Tags are properly formatted
      When the specification is generated
      Then it contains @deps:go tag at Feature level
      And it contains @L2 @ov tags at Scenario level
