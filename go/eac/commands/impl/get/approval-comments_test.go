package get

import (
	"testing"
)

func TestGetApprovalComments(t *testing.T) {
	// Basic test to verify command registration
	// Actual functionality is tested in core layer
	t.Run("command is registered", func(t *testing.T) {
		// Command should be registered via init()
		// This test just ensures the file compiles and imports work
	})
}
