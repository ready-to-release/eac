package workunit

import core "github.com/ready-to-release/eac/contracts/core/0.1.0"

// DisplayNameResolver computes shortest unique names within a set.
// Aliased from contracts/core/0.1.0 — the canonical definition.
type DisplayNameResolver = core.DisplayNameResolver

// NewDisplayNameResolver creates a resolver for the given set of units.
var NewDisplayNameResolver = core.NewDisplayNameResolver
