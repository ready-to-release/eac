// Package runner defines contract types for test, build, lint, and scan runner ports.
// This module provides the interface boundary between the execution engine (clibase)
// and the runner adapters, enabling adapters to depend on contracts instead of clibase.
package runner
