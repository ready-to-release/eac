# Default module configuration
# Used when .r2r/eac/modules.yml doesn't exist

modules:
  - moniker: default
    name: Default Module
    type: go
    description: Auto-generated module for unconfigured repositories
    files:
      root: "."
