{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "commit-message-module.schema.json",
  "title": "Commit Message Module Section",
  "description": "Structured module section data for multi-module commits",
  "type": "object",
  "required": ["module", "type", "description", "body"],
  "properties": {
    "module": {
      "type": "string",
      "pattern": "^[a-z][a-z0-9-]*$",
      "minLength": 1,
      "maxLength": 30,
      "description": "Module name (lowercase with hyphens)"
    },
    "type": {
      "type": "string",
      "enum": ["feat", "fix", "refactor", "docs", "chore", "test", "perf", "style", "ci", "build"],
      "description": "Semantic commit type for this module"
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "maxLength": 72,
      "description": "Brief description of changes in this module"
    },
    "body": {
      "type": "string",
      "minLength": 1,
      "description": "Detailed explanation of what changed in this module (2-4 sentences)"
    }
  }
}
