# Version: 0.1.0

moniker: "src-mcp-commands"
name: "Go Commands MCP Server"
type: "go-mcp"
description: "Internal MCP server for Commands"

source:
  changelog_path: "src/mcp/commands/CHANGELOG.md"
  root: "src/mcp/commands"
  specs_root: "specs/src-mcp-commands"
  includes:
    - "go.sum"
    - "go.mod"
    - "**.go"
    - "*.go"
    - "README.md"
    # Own the CI workflow file - triggers rebuild when src-mcp-commands or dependencies change
    - "/.github/workflows/src-mcp-commands.yaml"

depends_on:
  - "src-core"

used_by: []
