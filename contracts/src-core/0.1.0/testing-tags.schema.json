{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ready-to-release.org/schemas/src-core/testing-tags/0.1.0",
  "title": "Testing Tags Contract Schema",
  "description": "Schema for the test tagging system specification defining test classification and dependency management",
  "type": "object",
  "properties": {
    "metadata": {
      "$ref": "#/definitions/metadata"
    },
    "module_monikers": {
      "type": "object",
      "description": "Module moniker documentation (dynamically validated at runtime)",
      "properties": {
        "examples": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Example module monikers for documentation"
        }
      }
    },
    "environment_monikers": {
      "type": "object",
      "description": "Environment moniker documentation (dynamically validated at runtime)",
      "properties": {
        "examples": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Example environment monikers for documentation"
        }
      }
    },
    "system_dependency_names": {
      "type": "array",
      "description": "Valid system dependency names for @deps tags",
      "items": {
        "$ref": "#/definitions/system_dependency"
      }
    },
    "os_platform_names": {
      "type": "array",
      "description": "Valid OS platform names",
      "items": {
        "$ref": "#/definitions/os_platform"
      }
    },
    "tags": {
      "type": "array",
      "description": "Tag definitions for test classification",
      "items": {
        "$ref": "#/definitions/tag_definition"
      }
    },
    "types": {
      "type": "array",
      "description": "Tag type definitions",
      "items": {
        "$ref": "#/definitions/tag_type"
      }
    },
    "skip_reasons": {
      "type": "array",
      "description": "Valid skip reason codes",
      "items": {
        "$ref": "#/definitions/skip_reason"
      }
    }
  },
  "required": ["metadata", "tags", "types", "skip_reasons"],
  "definitions": {
    "metadata": {
      "type": "object",
      "description": "Contract metadata",
      "properties": {
        "version": {
          "type": "string",
          "description": "Contract version",
          "pattern": "^\\d+\\.\\d+\\.\\d+$"
        },
        "description": {
          "type": "string",
          "description": "Contract description"
        },
        "scope": {
          "type": "string",
          "description": "Scope of the contract"
        }
      },
      "required": ["version", "description"]
    },
    "system_dependency": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "System dependency identifier",
          "pattern": "^[a-z][a-z0-9-]*$"
        },
        "description": {
          "type": "string",
          "description": "Description of the dependency"
        }
      },
      "required": ["name", "description"]
    },
    "os_platform": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "OS platform identifier",
          "enum": ["linux", "macos", "windows", "os-agnostic"]
        },
        "description": {
          "type": "string",
          "description": "Description of the platform"
        }
      },
      "required": ["name", "description"]
    },
    "tag_definition": {
      "type": "object",
      "description": "A single tag definition",
      "properties": {
        "tag": {
          "type": "string",
          "description": "The tag string (may include placeholders like <reason>)",
          "pattern": "^@[a-zA-Z][a-zA-Z0-9-]*(:.*)?$"
        },
        "name": {
          "type": "string",
          "description": "Human-readable name for the tag"
        },
        "description": {
          "type": "string",
          "description": "Description of what the tag means"
        },
        "type": {
          "type": "string",
          "description": "Tag type category",
          "enum": [
            "system_dependency",
            "os_platform",
            "module_dependency",
            "environment",
            "taxonomy-level",
            "verification",
            "safety",
            "execution_control",
            "test_behavior",
            "risk_control",
            "risk_control_gxp",
            "gxp_regulatory"
          ]
        },
        "level": {
          "type": "string",
          "description": "Where the tag can be applied",
          "enum": ["feature-or-scenario", "scenario", "feature"]
        },
        "pattern": {
          "type": "string",
          "description": "Regex pattern for parameterized tags"
        },
        "example": {
          "type": "string",
          "description": "Example usage of the tag"
        },
        "note": {
          "type": "string",
          "description": "Additional notes about the tag"
        },
        "constraint": {
          "type": "string",
          "description": "Constraint rules for the tag",
          "enum": ["mutually_exclusive_with_taxonomy_levels"]
        }
      },
      "required": ["tag", "type"]
    },
    "tag_type": {
      "type": "object",
      "description": "Tag type category definition",
      "properties": {
        "type": {
          "type": "string",
          "description": "Type identifier"
        },
        "description": {
          "type": "string",
          "description": "Description of this tag type"
        }
      },
      "required": ["type", "description"]
    },
    "skip_reason": {
      "type": "object",
      "description": "Valid skip reason code",
      "properties": {
        "code": {
          "type": "string",
          "description": "Skip reason code",
          "pattern": "^[a-z]+$"
        },
        "name": {
          "type": "string",
          "description": "Human-readable name"
        },
        "description": {
          "type": "string",
          "description": "Description of when to use this skip reason"
        }
      },
      "required": ["code", "name", "description"]
    }
  },
  "examples": [
    {
      "metadata": {
        "version": "0.1.0",
        "description": "Test tagging system specification",
        "scope": "All test frameworks"
      },
      "tags": [
        {
          "tag": "@L0",
          "name": "Very Fast Unit Tests",
          "type": "taxonomy-level"
        },
        {
          "tag": "@skip:<reason>",
          "description": "Exclude from test runs",
          "type": "execution_control",
          "pattern": "^@skip:(?P<reason>[a-z]+)$",
          "example": "@skip:wip"
        }
      ],
      "types": [
        {
          "type": "taxonomy-level",
          "description": "Test environment complexity level"
        }
      ],
      "skip_reasons": [
        {
          "code": "wip",
          "name": "Work In Progress",
          "description": "Test implementation not yet complete"
        }
      ]
    }
  ]
}
